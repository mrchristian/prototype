{
  "name": "prototype-open-museum",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "bash -lc 'set -euo pipefail; python -m pip install --upgrade pip; pip install -r requirements.txt'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ]
    }
  }
}
